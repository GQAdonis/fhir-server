<!-- generated by kbd-runtime; source revision 260 -->
# agent-dev-team tasks

## add-hooks-typescript-toolchain — TypeScript 7 hook toolchain

- [x] 1 — 1.1 Create `.claude/hooks/package.json` (private, `typescript@7.0.2`, `@types/node@^24` devDependencies; scripts `build` (COMPLETE)
- [x] 2 — 1.2 Add `.claude/hooks/.gitignore` ignoring only `node_modules/`; verify `git check-ignore .claude/hooks/dist/x.mjs` rep (COMPLETE)
- [x] 3 — 2.1 Implement `src/lib/hook-io.mts` (stdin JSON reader, `allow`/`deny`/`feedback`/`context`/`warn` helpers, exit 0/2 sem (COMPLETE)
- [x] 4 — 2.2 Add `node:test` suites in `test/` covering malformed stdin, deny exit code, `additionalContext` JSON shape, and Wind (COMPLETE)
- [x] 5 — 3.1 Implement `scripts/check-dist.mjs` (compile to a temp outDir, byte-compare with `dist/`, list stale files); verify i (COMPLETE)
- [x] 6 — 3.2 Create `.claude/settings.json` with an empty `hooks` object and a `$schema` reference; verify it parses with `node - (COMPLETE)
- [x] 7 — 4.1 Add the Apache 2.0 WSO2 header to `internal/config/example_parse_test.go`; verify `grep -L 'Apache License' $(git ls (COMPLETE)

## add-architecture-agents — Architecture persona agents

- [x] 1 — 1.1 Vendor `karpathy-guidelines` into `.claude/skills/karpathy-guidelines/` with a source/re-sync note; verify `SKILL.md (COMPLETE)
- [x] 2 — 2.1 Write `.claude/agents/fhir-tech-lead.md` (opus; Agent + read-only tools; owns execute/status/goal-check/next-phase;  (COMPLETE)
- [x] 3 — 2.2 Write `.claude/agents/fhir-ideation-strategist.md` (opus; read + web research; owns new-phase/goal, brainstorming, i (COMPLETE)
- [x] 4 — 2.3 Write `.claude/agents/fhir-architect.md` (opus; design-path writes; owns assess/analyze/plan and OpenSpec authoring; (COMPLETE)
- [x] 5 — 3.1 Invoke each of the three agents through the Agent tool with a one-line role-confirmation prompt; verify each replies (COMPLETE)

## add-karpathy-agent-ledger — Karpathy agent ledger and boundary bridge

- [x] 1 — 1.1 Implement `src/lib/ledger.mts` (allowlisted field builder, sha256 prompt hash, single-line append, KBD phase/change  (COMPLETE)
- [x] 2 — 1.2 Implement `src/agent-ledger.mts` hook entry for SubagentStart/SubagentStop/PostToolUseFailure/TaskCompleted/UserProm (COMPLETE)
- [x] 3 — 2.1 Implement `src/lib/scan.mts` `scanText()` (SSN, MRN-like, email, phone, Bearer, `sk-`, credentialed DSN; optional re (COMPLETE)
- [x] 4 — 3.1 Implement `src/karpathy-flush.mts` (cursor-based ledger delta → markdown note in memory → `scanText` → temp write +  (COMPLETE)
- [x] 5 — 3.2 Verify live: with `pk` present, run a flush and confirm `pk list` shows the note; with a seeded SSN fixture confirm  (COMPLETE)
- [x] 6 — 4.1 Implement `src/karpathy-boundary.mts` (recorder and interpreter resolution per design, pass-through of `KBD_HOOK_*`  (COMPLETE)
- [x] 7 — 4.2 Create `.kbd-orchestrator/hooks-config.json` with three `augment` entries (task/change/phase `:after`) running the N (COMPLETE)
- [x] 8 — 5.1 Register `agent-ledger` and `karpathy-flush` in `.claude/settings.json` in exec form (flush on Stop, SessionEnd, Pre (COMPLETE)

## add-guardrail-hooks — Guardrail hooks

- [x] 1 — 1.1 Implement `src/lib/protected-paths.mts` (globs + per-glob reason, `*`/`**` matcher over project-relative POSIX paths (COMPLETE)
- [x] 2 — 2.1 Implement `src/license-header.mts` PostToolUse hook for `*.go`; verify tests give feedback for a headerless fixture  (COMPLETE)
- [x] 3 — 2.2 Implement `src/gofmt-check.mts` PostToolUse hook (10s timeout, silent when gofmt absent); verify tests with an unfor (COMPLETE)
- [x] 4 — 3.1 Implement `src/session-context.mts` SessionStart hook (phase, `nextChange`, position-reminder head, 2 KB cap, silent (COMPLETE)
- [x] 5 — 4.1 Register all four hooks in `.claude/settings.json` (exec form; PreToolUse/PostToolUse matcher `Edit|Write|MultiEdit` (COMPLETE)

## add-engineering-agents — Engineering persona agents

- [x] 1 — 1.1 Write `.claude/agents/fhir-go-developer.md` (sonnet, `isolation: worktree`, full edit tools; golang-patterns, tdd-wo (COMPLETE)
- [x] 2 — 1.2 Write `.claude/agents/fhir-storage-search-engineer.md` (opus, `effort: high`; all six storage/search rules with DESI (COMPLETE)
- [x] 3 — 1.3 Write `.claude/agents/fhir-test-engineer.md` (sonnet; testutil, build tags, race-integration parity command, UPDATE_ (COMPLETE)
- [x] 4 — 2.1 Ask `fhir-storage-search-engineer` through the Agent tool how it would handle an unsupported composite search parame (COMPLETE)
- [x] 5 — 2.2 Ask `fhir-test-engineer` for the command that mirrors CI race coverage; verify it returns `go test -race -tags integ (COMPLETE)

## add-quality-agents — Quality persona agents

- [x] 1 — 1.1 Write `.claude/agents/fhir-code-reviewer.md` (sonnet; `tools: Read, Grep, Glob, Bash`; `disallowedTools: Edit, Write (COMPLETE)
- [x] 2 — 1.2 Write `.claude/agents/fhir-security-compliance-reviewer.md` (opus; read-only; PHI/HIPAA, RLS/tenant, SQL injection i (COMPLETE)
- [x] 3 — 1.3 Write `.claude/agents/fhir-conformance-validator.md` (sonnet; read-only; opsx:verify, kbd-goal-check, verification-b (COMPLETE)
- [x] 4 — 2.1 Give `fhir-code-reviewer` a seeded diff that silently drops an unsupported search parameter; verify it returns a CRI (COMPLETE)
- [x] 5 — 2.2 Ask `fhir-conformance-validator` for its verification command list for a change touching `internal/store`; verify it (COMPLETE)

## add-infra-and-knowledge-agents — Infra and knowledge agents

- [x] 1 — 1.1 Write `.claude/agents/fhir-infra-release-engineer.md` (sonnet; edit scope `.github/`, `helm/`, `Dockerfile`, `docker (COMPLETE)
- [x] 2 — 1.2 Write `.claude/agents/fhir-knowledge-curator.md` (haiku; writes only `.prometheus/**` and `reflection.md` Karpathy s (COMPLETE)
- [x] 3 — 2.1 Give `fhir-knowledge-curator` a fixture ledger of five lines; verify it produces a session summary with no prompt/to (COMPLETE)

## document-and-verify-agent-team — Document and verify agent team

- [x] 1 — 1.1 Write `docs/agent-team.md` (roster + model rationale, persona→KBD/skill matrix, hand-off diagram, prerequisites YAML (COMPLETE)
- [x] 2 — 1.2 Add an "Agent team" section to `CLAUDE.md` (pointer to the doc, `claude --agent fhir-tech-lead`, hook build commands (COMPLETE)
- [x] 3 — 2.1 Implement `src/lint-agents.mts` and `lint:agents` npm script (frontmatter subset parser, model alias check, skills r (COMPLETE)
- [x] 4 — 3.1 Add `.github/workflows/agent-tooling.yml` (ubuntu/macos/windows matrix, Node 24, SHA-pinned checkout/setup-node, pat (COMPLETE)
- [x] 5 — 4.1 Verify all 11 `fhir-*` agents appear in Claude Code's agent list for this repo and each answers a role-confirmation  (COMPLETE)
- [x] 6 — 4.2 Run `/kbd-goal-check agent-dev-team`; verify G1–G5 and constraints C1–C2 report MET, or record the remaining gaps as (COMPLETE)
