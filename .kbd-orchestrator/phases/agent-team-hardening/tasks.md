<!-- generated by kbd-runtime; source revision 507 -->
# agent-team-hardening tasks

## install-domain-skills — Install and vendor healthcare skills, author project skills, record Firecrawl research

- [x] 1 — install-domain-skills 1 (COMPLETE)
- [ ] 1.1 — Write `.kbd-orchestrator/phases/agent-team-hardening/evidence/skill-research.md`: Firecrawl queries, result URLs, proven (PENDING)
- [x] 2 — install-domain-skills 2 (COMPLETE)
- [x] 3 — install-domain-skills 3 (COMPLETE)
- [x] 4 — install-domain-skills 4 (COMPLETE)
- [x] 5 — install-domain-skills 5 (COMPLETE)
- [x] 6 — install-domain-skills 6 (COMPLETE)
- [ ] 2.1 — Add `anthropics/healthcare` to `extraKnownMarketplaces` and enable `healthcare@healthcare` in `.claude/settings.json` (p (PENDING)
- [ ] 3.1 — Vendor the selected `ajhcs/healthcare-agents` skills (pinned commit, Apache-2.0) into `.agents/skills/`, and the `PhenoM (PENDING)
- [ ] 3.2 — Write `THIRD_PARTY_NOTICES.md` and `.agents/skills/SOURCES.json` (url, commit, license, roles for every third-party skil (PENDING)
- [ ] 4.1 — Author `ehr-integration-onboarding`, `fhir-data-sync-runbook`, `payer-documentation-rules` (cite-sources rule) and `phi- (PENDING)
- [ ] 5.1 — Mirror all repository skills to `.claude/skills/`, `.opencode/skills/`, `.kimi-code/skills/`, `.minimax/skills/` (script (PENDING)

## define-portable-team-manifest — Portable agent-team manifest (14 roles) as single source of truth

- [x] 1 — define-portable-team-manifest 1 (COMPLETE)
- [ ] 1.1 — Run `agent-team-models` discovery per harness (Claude, Codex, OpenCode, Kimi, MiniMax) and record concrete model ids per (PENDING)
- [x] 2 — define-portable-team-manifest 2 (COMPLETE)
- [x] 3 — define-portable-team-manifest 3 (COMPLETE)
- [x] 4 — define-portable-team-manifest 4 (COMPLETE)
- [x] 5 — define-portable-team-manifest 5 (COMPLETE)
- [x] 6 — define-portable-team-manifest 6 (COMPLETE)
- [x] 7 — define-portable-team-manifest 7 (COMPLETE)
- [ ] 1.2 — Create owned doc paths `docs/integrations/`, `docs/sync/`, `docs/billing/`, `docs/compliance/` with README stubs stating (PENDING)
- [ ] 2.1 — Port the 9 kept engineering prompts into `.agent-team/roles/<id>.md` (header + body), folding ideation into architect an (PENDING)
- [ ] 2.2 — Author the 5 domain role files (`hipaa-privacy-officer`, `fhir-integration-specialist`, `ehr-integration-manager`, `data (PENDING)
- [ ] 3.1 — Implement `scripts/agent-team/build-manifest.mjs` (roles → `team.json`, renders PHI-lane block + Harness card) with `nod (PENDING)
- [ ] 3.2 — Build `.agent-team/team.json`, run `cli.mjs validate` and `guide` (with the ownership map → `ready: true`), and `cli.mjs (PENDING)
- [ ] 3.3 — Add checks: disjoint ownership, skills resolve, every role has PHI-lane block and 5-harness Harness card; verify the che (PENDING)

## export-team-to-harnesses — Generate and install agents for Claude, Codex, OpenCode, Kimi, MiniMax Code

- [x] 1 — export-team-to-harnesses 1 (COMPLETE)
- [ ] 1.1 — Export the manifest to staging for claude, codex, opencode, kimi, minimax (`cli.mjs export`, new dirs under `.agent-team (PENDING)
- [x] 2 — export-team-to-harnesses 2 (COMPLETE)
- [x] 3 — export-team-to-harnesses 3 (COMPLETE)
- [x] 4 — export-team-to-harnesses 4 (COMPLETE)
- [x] 5 — export-team-to-harnesses 5 (COMPLETE)
- [x] 6 — export-team-to-harnesses 6 (COMPLETE)
- [x] 7 — export-team-to-harnesses 7 (COMPLETE)
- [ ] 1.2 — Review diagnostics per harness and apply role `native.<target>` model/tool overrides from `evidence/model-map.md`; verif (PENDING)
- [ ] 2.1 — Implement `scripts/agent-team/install-exports.mjs` (copies only team role files, refuses non-team files, idempotent) wit (PENDING)
- [ ] 2.2 — Install into `.claude/agents/`, `.codex/agents/`, `.opencode/agents/`, `.kimi-code/agents/`, `.minimax/agents/`; record  (PENDING)
- [ ] 2.3 — Implement `gen-agents-md` and generate `AGENTS.md` from `CLAUDE.md`; verify regeneration is byte-identical (PENDING)
- [ ] 3.1 — Extend `lint:agents` with per-harness structure checks, the card/PHI-lane check, and the drift check (temp export vs ins (PENDING)
- [ ] 3.2 — Verify loading per harness: fresh `claude` session lists 14 agents; `opencode agent list`; `codex` smoke (`-c` with agen (PENDING)

## retire-merged-agents — Retire fhir-ideation-strategist and fhir-knowledge-curator after merge

- [x] 1 — retire-merged-agents 1 (COMPLETE)
- [ ] 1.1 — Remove any remaining `fhir-ideation-strategist` / `fhir-knowledge-curator` definitions from all five harness locations a (PENDING)
- [x] 2 — retire-merged-agents 2 (COMPLETE)
- [x] 3 — retire-merged-agents 3 (COMPLETE)
- [ ] 1.2 — Update references (tech-lead hand-off map, `docs/agent-team.md` roster with a redirect note, CLAUDE.md/AGENTS.md if name (PENDING)
- [ ] 1.3 — Verify `lint:agents` (incl. drift check) passes and `openspec validate retire-merged-agents --strict` passes (PENDING)

## wire-hooks-per-harness — Wire guard and ledger hooks into each harness's native hook mechanism

- [x] 1 — wire-hooks-per-harness 1 (COMPLETE)
- [ ] 1.1 — Verify each harness's hook mechanism against its installed version (Codex hooks events and payload; OpenCode plugin API  (PENDING)
- [x] 2 — wire-hooks-per-harness 2 (COMPLETE)
- [x] 3 — wire-hooks-per-harness 3 (COMPLETE)
- [x] 4 — wire-hooks-per-harness 4 (COMPLETE)
- [x] 5 — wire-hooks-per-harness 5 (COMPLETE)
- [x] 6 — wire-hooks-per-harness 6 (COMPLETE)
- [ ] 2.1 — Implement `.claude/hooks/src/adapters/{codex,opencode,kimi}.mts` mapping native payloads to `hook-io` input and native d (PENDING)
- [ ] 2.2 — Register adapters: Codex hook config (project), `.opencode/plugin/fhir-guards.js` calling the compiled adapters, Kimi co (PENDING)
- [ ] 3.1 — Per supported harness, run a live or fixture session: a protected-path edit is denied and one ledger line is appended; r (PENDING)
- [ ] 3.2 — Write the capability matrix into `docs/agent-team.md`; verify it covers all five harnesses and matches `evidence/hook-ca (PENDING)
- [ ] 4.1 — De-flake karpathy-boundary interpreter probe timeout (PENDING)

## configure-phi-lanes — Tribe-endpoint PHI lane templates and phi-lane-guard (synthetic default)

- [ ] 1.1 — Implement `src/phi-lane-guard.mts` + `src/lib/phi-lane.mts` (URL extraction from WebFetch/MCP/Bash, FHIR-path match, san (PENDING)
- [ ] 1.2 — Register `phi-lane-guard` in `.claude/settings.json` and every supported adapter from `wire-hooks-per-harness`; verify c (PENDING)
- [ ] 2.1 — Write `.opencode/opencode.phi.template.json`, `.codex/config.phi.template.toml`, `.kimi-code/config.phi.template.toml` u (PENDING)
- [ ] 3.1 — Obtain `hipaa-privacy-officer` review (headless persona run with allowed read-only tools) of guard, allowlist, templates (PENDING)
- [ ] 3.2 — If any manifest role prompt changed, re-export all harnesses and run the drift check; verify `lint:agents` passes (PENDING)
- [ ] 4.1 — Tribe harness profiles deny off-lane channels (web, MCP, plugins, KB sinks, telemetry) (PENDING)
- [ ] 4.2 — Ledger rotation script and ledger-key allowlist in scan-prometheus (PENDING)

## document-and-verify-cross-harness-team — Docs, CI, goal check and per-harness acceptance

- [ ] 1.1 — Rewrite `docs/agent-team.md` (14-role roster, harness × tier model table, PHI-lane rules, hook capability matrix, per-ha (PENDING)
- [ ] 1.2 — Update `CLAUDE.md` Agent-team section and regenerate `AGENTS.md`; update `.kbd-orchestrator/project.json` `agents_config (PENDING)
- [ ] 2.1 — Extend `.github/workflows/agent-tooling.yml` with manifest validate, ownership/card checks, drift check, and phi-lane-gu (PENDING)
- [ ] 3.1 — Run per-harness smoke prompts for the 5 domain roles on synthetic data (Claude, Codex, OpenCode, Kimi headless; MiniMax  (PENDING)
- [ ] 3.2 — Run the phase goal check via `fhir-conformance-validator` (including re-checking the three unvetted plan round-2 fixes); (PENDING)
